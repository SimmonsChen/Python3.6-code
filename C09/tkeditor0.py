from tkinter import *
# 创建主窗口
root = Tk()
#设置窗口标题
root.title('tkeditor')
# 设置窗口大小可调性，分别表示x,y方向的可变性，0,0表示窗口不可变
root.resizable(0, 0)
# 设定初始窗口大小
root.geometry('450x300')
# 这里用于添加我们的代码
# 消息循环
root.mainloop()
